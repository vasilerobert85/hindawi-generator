const initialize = ({ logger, models, services }) => ({
  async execute(input) {
    return 'Add your own use case here.'
  },
})

const authsomePolicies = []

module.exports = {
  initialize,
  authsomePolicies,
}
